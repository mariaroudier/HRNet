import { PaginationState, PaginationMeta } from "./getPaginationMeta";
declare type UsePaginationConfig = {
    totalItems?: number;
    initialPage?: number;
    initialPageSize?: number;
};
declare type PaginationActions = {
    setPage: (page: number) => void;
    setNextPage: () => void;
    setPreviousPage: () => void;
    setPageSize: (pageSize: number, nextPage?: number) => void;
};
export declare function usePagination({ totalItems, initialPage, initialPageSize, }?: UsePaginationConfig): PaginationState & PaginationMeta & PaginationActions;
export {};
